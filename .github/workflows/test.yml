name: test-priv-registry

on:
  push:
    branches: [ main ]

jobs:
  run_pv_docker_image:
    container:
      image: 462986224002.dkr.ecr.us-east-1.amazonaws.com/celio-test-orange
      credentials:
        username: AWS
        password: ${{ secrets.pass}}